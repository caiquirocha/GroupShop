<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionMenuTextColor">@color/colorMenuText</item>
    </style>

    <style name="TextLine" parent="Theme.AppCompat.Light">
        <!-- Hint Color -->
        <item name="android:textColorHint">@color/colorTextInputHint</item>
        <!-- Bar Color -->
        <item name="colorControlNormal">@color/colorTextInputBar</item>
        <!-- Bar hint focused Color -->
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>

        <item name="textColorError">@color/colorError</item>
    </style>

    <style name="TextAppearence.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorInputHintFloat</item>
    </style>

    <style name="AppTheme.PrimaryButton" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorButtonNormal">@color/colorPrimary</item>
    </style>

    <!--
    <style name="AppTheme.CheckBox" parent="Base.Theme.AppCompat">
        <item name="colorAccent"></item>
        <item name="android:textColorSecondary"></item>
    </style>
    -->


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
